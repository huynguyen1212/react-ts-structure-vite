/*
 * Template  Messages
 *
 * This contains all the text for the Template component.
 */

import { defineMessages } from "react-intl";

export const scope = "Template";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
